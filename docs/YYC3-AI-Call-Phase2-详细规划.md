# YYC³ AI Intelligent Calling - Phase 2 详细规划

**规划时间**: 2026-01-23 15:00  
**阶段**: Phase 2 - 数据库集成 + E2E 测试 + 生产优化  
**预计周期**: 2-3 周  
**优先级**: 高

---

## 📋 Phase 2 工作分解 (WBS)

### 2.1 数据库集成 (High Priority)

#### 2.1.1 PostgreSQL 环境搭建

```bash
# 目标: 在 Docker 环境启动 PostgreSQL 数据库，支持本地开发
# 预期输出:
#   - docker-compose.yml 包含 PostgreSQL 15 配置
#   - .env.local 包含数据库连接字符串
#   - 初始化脚本自动创建必要的角色和数据库

# 执行步骤:
# 1. 增强 docker-compose.yml
#    - 添加 PostgreSQL 服务
#    - 配置卷持久化数据
#    - 设置初始化脚本
# 2. 创建 database/init.sql
#    - 创建数据库用户
#    - 创建基础表结构
# 3. 更新 .env.example
#    - DATABASE_URL=postgresql://user:pass@localhost:5432/yyc3_dev
```

**实现清单**:

- [ ] 增强 docker-compose.yml (PostgreSQL 15 + pgAdmin)
- [ ] 创建 database/init.sql 初始化脚本
- [ ] 配置 .env 变量管理
- [ ] 验证本地数据库连接

#### 2.1.2 Prisma ORM 集成

```bash
# 目标: 使用 Prisma ORM 管理数据库架构和操作
# 预期输出:
#   - prisma/schema.prisma 完整的数据模型定义
#   - Prisma 客户端配置
#   - 类型安全的数据库操作

# 核心数据模型:
# - User (用户)
# - Customer (客户)
# - CallRecord (通话记录)
# - Campaign (营销活动)
# - Form (表单)
# - Task (任务/跟进)
# - AnalyticsMetric (分析指标)

# 依赖安装:
pnpm add @prisma/client
pnpm add -D prisma
```

**实现清单**:

- [ ] 安装 @prisma/client 和 prisma
- [ ] 创建 prisma/schema.prisma 数据模型
- [ ] 初始化 Prisma 客户端
- [ ] 配置 Prisma 生成客户端

#### 2.1.3 数据库迁移管理

```bash
# 目标: 使用 Prisma Migrate 管理数据库架构版本
# 预期输出:
#   - prisma/migrations/* 迁移文件
#   - 自动化迁移脚本

# 常用命令:
pnpm prisma migrate init           # 初始化迁移系统
pnpm prisma migrate dev            # 开发环境迁移
pnpm prisma migrate deploy         # 生产环境迁移
pnpm prisma studio                 # 启动 Prisma Studio (GUI)
```

**实现清单**:

- [ ] 初始化 Prisma 迁移系统
- [ ] 创建初始迁移文件
- [ ] 验证 dev 环境迁移
- [ ] 配置生产迁移策略

#### 2.1.4 种子数据 (Seeding)

```bash
# 目标: 提供开发/测试数据集
# 文件结构:
# prisma/seed.ts - 种子脚本
# scripts/seed.sh - 执行脚本

# 种子内容:
# - 管理员用户 x1
# - 测试客户 x50
# - 样本通话记录 x200
# - 营销活动 x5
# - 智能表单 x3

# 执行:
pnpm prisma db seed
```

**实现清单**:

- [ ] 创建 prisma/seed.ts 脚本
- [ ] 定义测试数据生成逻辑
- [ ] 配置 package.json 中的 seed 命令
- [ ] 验证数据导入完整性

### 2.2 E2E 测试框架 (High Priority)

#### 2.2.1 Playwright 环装置

```bash
# 目标: 搭建 Playwright E2E 测试框架
# 预期输出:
#   - playwright.config.ts 配置文件
#   - tests/e2e/** 测试脚本
#   - CI 集成配置

# 依赖安装:
pnpm add -D @playwright/test
pnpm exec playwright install
```

**实现清单**:

- [ ] 安装 @playwright/test
- [ ] 创建 playwright.config.ts 配置
- [ ] 设置测试目录 (tests/e2e/\*\*)
- [ ] 配置浏览器 (Chrome, Firefox, Safari)
- [ ] 设置基础 URL (http://localhost:3000)
- [ ] 配置 CI 并行执行

#### 2.2.2 测试场景编写

```typescript
// tests/e2e/01-auth.spec.ts - 认证流程测试
test("用户登录流程", async ({ page }) => {
  // 1. 导航到首页
  // 2. 点击登录按钮
  // 3. 输入凭证
  // 4. 验证登录成功
  // 预期: 重定向到 /dashboard
});

// tests/e2e/02-customer.spec.ts - 客户管理流程
test("创建新客户 - 完整流程", async ({ page }) => {
  // 1. 导航到客户管理页
  // 2. 点击新增客户
  // 3. 填写表单
  // 4. 保存并验证
  // 预期: 新客户出现在列表中
});

// tests/e2e/03-call.spec.ts - 外呼流程测试
test("智能外呼系统 - 完整通话流程", async ({ page }) => {
  // 1. 导航到智能外呼系统
  // 2. 点击开始录音
  // 3. 验证计时器运行
  // 4. 停止录音
  // 5. 验证分析数据
  // 预期: 显示情感分析和脚本推荐
});
```

**实现清单**:

- [ ] 创建认证流程测试 (01-auth.spec.ts)
- [ ] 创建客户管理流程测试 (02-customer.spec.ts)
- [ ] 创建外呼系统流程测试 (03-call.spec.ts)
- [ ] 创建数据分析流程测试 (04-analytics.spec.ts)
- [ ] 创建营销自动化流程测试 (05-marketing.spec.ts)
- [ ] 验证跨浏览器兼容性

#### 2.2.3 Playwright CI 集成

```yaml
# .github/workflows/e2e.yml
name: E2E Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
      - uses: actions/setup-node@v3
      - run: pnpm install
      - run: pnpm build
      - run: pnpm start &
      - run: pnpm playwright install
      - run: pnpm exec playwright test
```

**实现清单**:

- [ ] 创建 .github/workflows/e2e.yml
- [ ] 配置 CI 并行执行 (workers)
- [ ] 设置失败重试机制
- [ ] 配置视频记录 (失败时)
- [ ] 配置 HTML 报告生成

### 2.3 API 数据持久化 (High Priority)

#### 2.3.1 客户管理 API

```typescript
// app/api/customers/route.ts
// GET /api/customers - 获取客户列表 (数据库查询)
// POST /api/customers - 创建客户 (数据库保存)

// app/api/customers/[id]/route.ts
// GET /api/customers/[id] - 获取客户详情
// PUT /api/customers/[id] - 更新客户信息
// DELETE /api/customers/[id] - 删除客户
```

**实现清单**:

- [ ] 重写 GET /api/customers (添加 Prisma 查询)
- [ ] 实现 POST /api/customers (数据库保存 + 验证)
- [ ] 创建 [id]/route.ts (GET/PUT/DELETE)
- [ ] 添加错误处理和事务管理
- [ ] 添加审计日志 (创建/修改/删除时间)

#### 2.3.2 通话记录 API

```typescript
// app/api/calls/route.ts
// GET /api/calls - 获取通话记录列表
// POST /api/calls - 创建通话记录

// app/api/calls/[id]/route.ts
// GET /api/calls/[id] - 获取通话详情
// PUT /api/calls/[id] - 更新通话信息 (如添加分析结果)
```

**实现清单**:

- [ ] 创建 app/api/calls/route.ts
- [ ] 创建 app/api/calls/[id]/route.ts
- [ ] 集成 AI 分析结果存储
- [ ] 实现分页和过滤

#### 2.3.3 表单提交 API

```typescript
// app/api/forms/[id]/submissions
// POST - 提交表单数据到数据库
// GET - 获取表单提交记录
```

**实现清单**:

- [ ] 创建表单提交端点
- [ ] 数据验证和清理
- [ ] 存储提交元数据

### 2.4 生产优化 (Medium Priority)

#### 2.4.1 性能监控

```bash
# 目标: 集成 Web Vitals 监控
# 工具: Sentry (错误追踪) + Web Vitals

# 依赖:
pnpm add @sentry/nextjs @sentry/tracing web-vitals
```

**实现清单**:

- [ ] 集成 Sentry 错误追踪
- [ ] 配置 Web Vitals 收集
- [ ] 设置性能监控告警

#### 2.4.2 缓存策略

```typescript
// 目标: 实现 Redis 缓存层
// 场景:
// - 客户列表缓存 (5 分钟)
// - 热点查询缓存
// - 会话存储

// 实现: lib/cache.ts
export async function getCachedCustomers(page: number) {
  const cacheKey = `customers:${page}`;
  const cached = await redis.get(cacheKey);
  if (cached) return JSON.parse(cached);

  const data = await prisma.customer.findMany({
    skip: (page - 1) * 10,
    take: 10,
  });
  await redis.setex(cacheKey, 300, JSON.stringify(data));
  return data;
}
```

**实现清单**:

- [ ] 配置 Redis 客户端 (lib/redis.ts)
- [ ] 实现缓存装饰器
- [ ] 配置缓存失效策略

#### 2.4.3 安全强化

```bash
# 目标: 提升生产环保全性
# 措施:
# - API 速率限制 (Rate Limiting)
# - CORS 配置
# - CSRF 防护
# - SQL 注入防护 (Prisma 已内置)
# - 敏感数据加密

# 依赖:
pnpm add next-cors ratelimit

# 环境变量:
# - DATABASE_PASSWORD 加密
# - API_SECRET_KEY (JWT signing)
# - ENCRYPTION_KEY (敏感字段)
```

**实现清单**:

- [ ] 配置 API 速率限制
- [ ] 设置 CORS 策略
- [ ] 实现 CSRF 中间件
- [ ] 加密敏感字段 (密码、电话等)

### 2.5 文档和部署准备 (Medium Priority)

#### 2.5.1 数据库文档

```markdown
# 数据库架构文档

- ER 图
- 表定义和关系
- 数据库维护指南
- 备份策略
```

**实现清单**:

- [ ] 创建 ER 图 (Lucidchart 或 Dbdiagram)
- [ ] 编写数据库架构文档
- [ ] 记录迁移历史

#### 2.5.2 E2E 测试文档

```markdown
# E2E 测试指南

- Playwright 配置说明
- 编写新测试的步骤
- 调试测试失败
- CI 集成说明
```

**实现清单**:

- [ ] 编写 Playwright 使用指南
- [ ] 创建测试编写模板
- [ ] 记录常见问题排查

#### 2.5.3 部署清单

```markdown
# 生产部署检查列表

- [ ] 环境变量配置完整
- [ ] 数据库备份策略就位
- [ ] 监控和告警配置
- [ ] 性能基准测试完成
- [ ] 安全审计通过
- [ ] 灾备计划制定
```

**实现清单**:

- [ ] 创建部署检查列表
- [ ] 编写灾备恢复指南
- [ ] 配置自动备份脚本

---

## 📊 Phase 2 时间分配

| 子任务              | 时间估算    | 难度 | 依赖       |
| ------------------- | ----------- | ---- | ---------- |
| PostgreSQL + Docker | 2-3天       | 中   | -          |
| Prisma ORM 集成     | 2-3天       | 中   | PostgreSQL |
| 数据迁移            | 1-2天       | 低   | Prisma     |
| 种子数据            | 1天         | 低   | Prisma     |
| Playwright 设置     | 1-2天       | 低   | -          |
| E2E 测试编写        | 3-4天       | 中   | Playwright |
| API 持久化          | 3-5天       | 中   | Prisma     |
| 缓存层 (Redis)      | 1-2天       | 中   | API        |
| 安全加固            | 1-2天       | 中   | -          |
| 文档 & 部署         | 1-2天       | 低   | -          |
| **总计**            | **16-26天** | -    | -          |

---

## 🎯 Phase 2 成功指标

### 功能完成度

- [ ] PostgreSQL 数据库完全集成
- [ ] Prisma ORM 实现数据模型 7+ 个表
- [ ] API 层 100% 使用数据库 (非 mock 数据)
- [ ] 种子数据包含 1000+ 条测试数据

### 测试覆盖

- [ ] E2E 测试 5+ 个关键流程
- [ ] 测试覆盖 8 大前端模块
- [ ] 所有 API 端点有单元测试
- [ ] E2E 在 CI 上自动运行

### 性能目标

- [ ] API 响应时间 < 200ms (缓存命中)
- [ ] 构建时间 < 30s
- [ ] Lighthouse 分数 > 90
- [ ] 数据库查询 < 100ms

### 质量指标

- [ ] 代码覆盖率 > 85%
- [ ] ESLint + TypeScript 0 errors
- [ ] 安全审计通过
- [ ] 文档完整度 100%

---

## 📌 关键路径 (Critical Path)

```
┌─────────────────────┐
│ PostgreSQL Setup    │ (2-3 days)
│ + Docker Config     │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ Prisma ORM Setup    │ (2-3 days)
│ + Data Models       │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ API Persistence     │ (3-5 days)
│ (Rewrite w/ DB)     │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ E2E Tests           │ (3-4 days)
│ + Playwright Setup   │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ Performance Tuning  │ (1-2 days)
│ + Redis Caching     │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ Deployment Ready    │ (1-2 days)
│ + Security Hardening│
└─────────────────────┘
```

**关键路径总长**: 13-19 天 (并行其他工作)

---

## 🚀 Phase 2 启动检查

启动 Phase 2 前验证以下条件：

- [x] Phase 1 所有任务完成且验收通过
- [x] 主 branch (main) 已发布到生产
- [x] 团队对 Prisma ORM 和 Playwright 有基本了解
- [ ] 本地开发环境已安装 Docker Desktop
- [ ] PostgreSQL 客户端 (psql 或 pgAdmin) 可用
- [ ] 所有开发人员 GitHub 账号已配置

---

## 📞 Phase 2 联系与支持

- **技术主管**: admin@0379.email
- **开发文档**: [项目 Wiki](https://github.com/YYC-Cube/yyc3-ai-call/wiki)
- **问题跟踪**: [GitHub Issues](https://github.com/YYC-Cube/yyc3-ai-call/issues)
- **讨论区**: [GitHub Discussions](https://github.com/YYC-Cube/yyc3-ai-call/discussions)

---

**规划完成**: 2026-01-23 15:00  
**阶段负责人**: 待指定  
**审核状态**: 📋 待审批
